// MongoDB Playground
// Creates a new collection in the local db called PHONE

const database = 'local';
const collection = 'PHONE';

// The current database to use.
use(database);

// Create a new collection.
db.createCollection(collection);
